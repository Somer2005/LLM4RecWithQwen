# 本项目是在传统的比赛模型基础上，加上了一条LLM召回通路的一类魔改推荐模型
为了保证整体流程干净，用12345678标识了数据流程。
下面将详细介绍每一个文件做了什么。

# data_preparation.py
这个函数里我们做了包括数据清洗，并且，把原来的数据集中那种One-hot的类型偏好转换成了字符串形式，当然这是我们在这个文件里做的事情。

同时我们还做了一件事，就是把职业和性别变成了离散ID；并构建了一个弱特征的，可以刻画用用户画像的函数，还有一个以用户画像，用户年纪，用户职业为特征的让LLM做推荐的函数，后面会用到。

# TwoTowerPreparation.py
构建embedding的代码我忘了放进来了……

反正双塔的模型嘛embedding大家自己也都会写，无非就是用户的embedding我们就把离散特征各自喂给一个embedding层，这个没什么好说的基本在data那个文件夹的python里面逻辑很清楚……

这个py里，我们利用用户历史行为数据池化求平均构建了用户初始embedding，而另一边的Item初始embedding我们使用了word2vec。

# 
